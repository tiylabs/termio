---
title: 简介
description: Termio 是一款原生 Mac 应用，为你的 AI 编程 Agent 提供工作台——Claude Code、Codex 以及其他 Agent 并排运行，各自待在真实终端里，跑在本机或你自己的机器上。
x-i18n:
  source_path: index.mdx
  source_hash: a84e3d11b4b297d4273f73fb7c89b26a123137fd984129d54369adfe5cce0256
  generated_at: 2026-09-19
---

Termio 是一款原生 Mac 应用，为你的 AI 编程 Agent 安了个家。你不必再在零散的终端
标签页之间来回倒腾，而是让它们并排运行——每个都在真实终端里——并且瞬间切换。
一切都跑在你自己的硬件上：本机，或者任何你 `ssh` 得上的机器。

## Termio 给你什么

- **每个 Agent 一个真实终端。** Claude Code、Codex、Amp、Droid、Cursor、Copilot、
  Antigravity、Crush、Cline 等等，都跑在真正的 PTY 里，而不是被重新渲染的聊天记录。
  你在 iTerm 里看到的样子，在这里一模一样。
- **一条会话侧栏。** 每个 Agent 和 shell 都只需一次点击。状态实时更新，哪个在
  工作、哪个空闲、哪个需要你，一眼就知道。
- **拆分窗格。** 把 Agent、开发服务器和 shell 放进同一个窗口，按你的思路排布。
- **任何一台你自己的机器。** 工作区可以放在 VPS 上，或者房间另一头的 Mac mini 上。
  Termio 通过 SSH 把一个二进制复制过去，把 Agent 配好；会话跑在那台机器上，你附不
  附着都照跑。
- **内置检查器。** 浏览工作目录、带语法高亮地编辑文件、读 git 差异、在整个项目
  里搜索——全都不用离开窗口。
- **一套编排 API。** `termio sessions` 命令行让一个 Agent（或你的脚本）通过本地
  套接字派生同伴、回答它们的提问、监督整支队伍，JSON 结构固定，退出码诚实。
- **把手机当遥控器。** iPhone 应用配对一次，之后随时随地查看正在运行的会话——
  走你自己的网络，或你自己掌控的隧道。

## 始终本地优先

Termio 没有账号，也不需要登录。你的终端、代码和 Agent 对话不会经过我们的服务器
——产品的数据路径里根本没有我们的服务器。连另一台机器走的是系统 `ssh` 和你自己的
`~/.ssh/config`；Termio 不开通任何机器，也不中转任何流量。你运行的 Agent 用你自己
的账号与它们各自的服务商通信，和在普通终端里完全一样。

Termio **免费使用**——没有账号，没有许可证密钥，也没有支付后端。

## 选一条路

大多数人来到这里，原因无非三种。从你真正所处的位置开始：

<Cards>
  <Card href="/zh-CN/docs/first-session" title="我只想先跑起来一个 Agent">
    安装、打开项目、在真实终端里启动 Claude Code 或 Codex，然后拆分窗口。十分钟，不需要
    先懂任何概念。
  </Card>
  <Card href="/zh-CN/docs/agents" title="我已经在同时跑好几个了">
    真正有回报的那部分：每个会话一份实时状态、用 worktree 让两个 Agent 不动同一批文件，
    以及把侧栏当成整支队伍的看板。
  </Card>
  <Card href="/zh-CN/docs/devices" title="我的代码在另一台机器上">
    一台 VPS、一台 Mac mini、一台开发机。Termio 通过你自己的 SSH 配置把一个二进制复制
    过去，会话就跑在那台机器上，无论你有没有附着。
  </Card>
</Cards>

## 或者让 Agent 替你读文档

你手上本来就有一个编程 Agent。这份文档以它能直接读的形式发布，所以你可以把安装这件事交给
它，而不是自己动手：

```text
Read https://termio.sh/llms.txt and then help me set up Termio for my project.
```

每一页也都能在自己的 URL 上以纯 Markdown 应答，而
[llms-full.txt](https://termio.sh/llms-full.txt) 是全部页面合成的一个文件——这样 Agent
就能照着文档回答，而不是自己编。

## 从这里开始

<Cards>
  <Card href="/zh-CN/docs/installation" title="安装">
    下载 Mac 应用并让它自动更新。无需注册账号。
  </Card>
  <Card href="/zh-CN/docs/first-session" title="你的第一个会话">
    打开项目，在真实终端里启动 Agent，然后拆分窗口。
  </Card>
  <Card href="/zh-CN/docs/agents" title="同时运行多个 Agent">
    让一支队伍并排工作，一眼读出每个 Agent 的实时状态。
  </Card>
  <Card href="/zh-CN/docs/devices" title="设备">
    把工作区放到另一台机器上，并在那儿装好 Termio server。
  </Card>
  <Card href="/zh-CN/docs/persistence" title="什么会留下来">
    关闭、退出、更新、重启——正在跑的 Agent 究竟会怎么样。
  </Card>
  <Card href="/zh-CN/docs/session-control" title="编排与命令行">
    用 `termio sessions` 让 Agent 互相派生、驱动与监督。
  </Card>
  <Card href="/zh-CN/docs/iphone" title="iOS 应用">
    配对手机，随时随地查看正在运行的 Agent。
  </Card>
</Cards>
